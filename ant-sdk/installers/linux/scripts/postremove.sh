#!/bin/sh
# antd .deb/.rpm post-remove scriptlet (runs as root).
# The unit file is removed by the package manager with the rest of the payload;
# nothing else to clean up. Kept for symmetry / future use.
set -e
exit 0
